import assign from 'react/lib/Object.assign';
import * as ReactBootstrap from './global/vendor/bootstrap';
import * as Sortable from './global/vendor/sortable';

assign(global, ReactBootstrap);
assign(global, Sortable);